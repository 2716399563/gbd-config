# Cloudflare Tunnel token（Zero Trust → Tunnels → 你的 tunnel → Install connector）
CLOUDFLARED_TUNNEL_TOKEN=

# 服务器上允许 MCP 读写的目录（建议为本仓库 clone 路径）
WORKSPACE_PATH=/opt/gbd-config

# MCP 网关监听端口（仅绑定容器内，由 cloudflared 转发）
MCP_PORT=8000
