# Cloudflare Tunnel token（Zero Trust → Tunnels → Install connector）
CLOUDFLARED_TUNNEL_TOKEN=

# 服务器上 MCP 读写目录（amd-radeon-register 项目）
WORKSPACE_PATH=/opt/amd-radeon-register

# MCP 网关端口（容器内，由 cloudflared 转发）
MCP_PORT=8000
